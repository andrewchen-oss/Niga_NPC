[project]
name = "skyeye-bot"
version = "1.0.0"
description = "Twitter/X AI Bot for Face Search and X Roast"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
